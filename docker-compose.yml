services:
  mysql:
    image: mysql:8.4
    container_name: lujuria-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: lujuria
      MYSQL_USER: lujuria
      MYSQL_PASSWORD: lujuria
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
